# go-ecs-utils

## building

```sh
go build -o ./bin/ecs-utils -v main.go
```

There is also a Makefile

```sh
# for OSX
make build-darwin

# cross-compile for RaspberryPi
make build-raspi
```

# usage

```sh
./bin/ecs-utils
```
